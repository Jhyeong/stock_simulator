import Layout from '../components/Layout';

const pageone = () => {

    return(
        <Layout>
            <div>
                페이지원
            </div>
        </Layout>
    );
}

export default pageone;